import { clienteProvider } from './ClienteProvider';
import { entregadorProvider } from './EntregadorProvider';
import { funcionarioProvider } from './FuncionarioProvider';
import { pedidoProvider } from './PedidoProvider';
import { produtoProvider } from './ProdutoProvider';

const api = {
  clienteProvider,
  entregadorProvider,
  funcionarioProvider,
  pedidoProvider,
  produtoProvider,
};

export default api;
